import React from 'react'

const Input = ({label}) => {
  return (
      <div>
        <p className="mt-8 mb-1 text-sm text-[#33475b] font-semibold">first name</p>
      <input type="text" placeholder="firstnme"  className="mt-2 mb-2 border-b-4"/>
      </div>

  )
}

export default Input